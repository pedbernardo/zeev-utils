import { setup } from './setup'

import {
  getField,
  getFieldContainer,
  getFieldValue,
  clearField,
  onFileChange
} from './utils/field'

import {
  isRequired,
  addRequired,
  removeRequired
} from './utils/require'

import {
  showField,
  hideField,
  showGroup,
  hideGroup
} from './utils/visibility'

export {
  setup,

  // Field
  getField,
  getFieldContainer,
  getFieldValue,
  clearField,
  onFileChange,

  // Requirement
  isRequired,
  addRequired,
  removeRequired,

  // Visibility
  showField,
  hideField,
  showGroup,
  hideGroup
}
